// Remove the reply element
var replyElement = document.querySelector('#reply-<%= @reply.id %>');
replyElement.remove();

var notification = document.getElementById('notification');
notification.innerHTML = '<%= j render "shared/notification", message: "Reply deleted successfully", type: "success" %>';
notification.classList.remove('hidden');

var closeButton = notification.querySelector('.close');
closeButton.addEventListener('click', function() {
    notification.classList.add('hidden');
});

setTimeout(function() {
    notification.classList.add('hidden');
}, 3000); 